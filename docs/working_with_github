#XXX TODO: make this fancy github markup

#Pretty much just follow this guide
#https://kbroman.org/github_tutorial/pages/first_time.html

#salient points for quick reference
#Cloud9 has `/usr/bin/nano` set to the editor by default. This will fail on an ubuntu image. 
git config --global core.editor vim

#quick reference for auth config

#Set your username and email
git config --global user.name "Your name here"
git config --global user.email "your_email@example.com"

#create an ssh key
ssh-keygen -t rsa -C "your_email@example.com"

#add public key to your ssh account. see github docs

#test
ssh -T git@github.com

